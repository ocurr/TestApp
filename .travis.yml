language: objective-c
xcode_scheme: TestAppTests

osx_image: xcode8.3

before_install:
    - gem install cocoapods -v '1.2.1'

install:
    - pod repo update
    - pod update
    - pod install

script: travis_wait xctool -workspace TestApp.xcworkspace -scheme TestApp -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO build test
